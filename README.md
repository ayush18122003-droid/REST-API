# REST-API
Project Overview: Book Management REST API ​This project implements a lightweight RESTful API built using Node.js and the Express framework to perform CRUD (Create, Read, Update, Delete) operations on an in-memory collection of books.
